# Force LF line endings on files that run in Linux containers,
# regardless of host OS (matters when devs use Windows).
*.sh         text eol=lf
*.toml       text eol=lf
*.tpl        text eol=lf
Dockerfile   text eol=lf
*.conf       text eol=lf
